externo ent hola();
externo ent mundo();
externo ent holamundo();
externo ent duplica(ent x);

público ent inicia()
{
    //ent[] vector = [0, 1, 2, 3];

    ent número = 288 * 2;

    //holamundo();
    
    si (42 < 2)
    {
        //ent y = duplica(42);
        ent y = duplica(número);
        número = y;
    }
    sino
    {
        número = número;
    }

    holamundo();

    devuelve número;
}