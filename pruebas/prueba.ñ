externo ent escribeln(nat8* _texto);

público ent inicia()
{
    // Caracteres "¡Hola mundo!\0"
    nat8[] texto1 = [194, 161, 72, 111, 108, 97, 32, 109, 117, 110, 100, 111, 33, 0];
    nat8[] texto2 = "¡Hola mundo 2!";

    //nat8* ptrTxt = texto1;

    //escribeln(texto);

    devuelve escribeln(texto2);
}