//externo nada escribe(ent número);
externo ent duplica(ent x);

público ent inicia()
{
    ent número = 42;

    si (42 < 2)
    {
        //ent y = duplica(42);
        ent y = duplica(número);
        número = y;
    }
    sino
    {
        número = número;
    }

    devuelve número;
}