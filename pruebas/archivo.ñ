externo nada escribe(ent número);
externo ent duplica(ent x);

público ent inicia()
{
    ent número = 42;

    si (42 > 2)
    {
        ent y = duplica(42);
        escribe(y);
    }
    sino
    {
        escribe(288);
    }

    devuelve 0;
}