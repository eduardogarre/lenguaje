externo ent hola();
externo ent duplica(ent x);

público ent inicia()
{
    ent número = 288;

    si (42 < 2)
    {
        //ent y = duplica(42);
        ent y = duplica(número);
        número = y;
    }
    sino
    {
        número = número;
    }

    devuelve hola();
}